package com.vivianhonghoa.chat.shared;

public class ToServerCommandRegistry {
    public static final Command JOIN = new Command("JOIN", 1);
    public static final Command EXIT = new Command("EXIT", 0);
}
